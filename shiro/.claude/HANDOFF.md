# Orchestration handoff — updated 2026-07-06 (work PAUSED by user; continuation scheduled 2026-07-07 03:00)

Read this if resuming without conversation context. Roles: Fable = Creative Director/orchestrator (no production code); implementation delegated to Sonnet agents — IN HEADLESS RUNS, RUN AGENTS SYNCHRONOUSLY (run_in_background: false); a backgrounded agent gets killed ~600s after the main turn ends (bit us on the first night). Source of truth: `/home/jiaowh/shiro/design-system.md`. Quality bar: Awwwards.

## PROJECT COMPLETE (2026-07-07, Opus session) — all 6 waves shipped + CD-approved
All of Waves 1–6 are done, committed, and verified in-browser by the Creative Director at 320/390/768/1440. Final review (task #8) passed. The site meets the Awwwards quality bar: S0 asymmetric organic-wash arrival with stroke-drawn title → S1/S2 paper chapters (rotating-ink margin notes, dwell reveals, archive board) → S3 cinema (burn handoff, woven Mincho kanji, 覚醒 clip-path hero, keyboard variant switcher, benched-single night rails) → S4 sepia materials → S5 finale (dusk deepen, 6/6 moon assembly + obi completion, reader-card postcard). Responsive: mobile top-thread rail (cinema-mode white-on-dark scrim, verified under real scroll), zero horizontal overflow 320–1440, plate asymmetry preserved single-column. A11y: keyboard/ARIA tablist/contrast/alt/headings/lang/reduced-motion all verified; focus ring retargeted to #D6342F (own token; clears WCAG 3:1 on both --night ~3.6:1 and --paper ~4.2:1; --accent-red untouched). Perf: 58.4KB gz JS, picture+lazy+srcset, LCP is inline SVG title. No console errors on full journeys. Git: Waves 1–5 at a737f27, Wave 6 + final review committed on top.
FOLLOW-UPS IF EVER RESUMED (all optional, non-blocking): sound design was an optional brief item, not built; moon-button↔mobile-rail proximity at S0 is close-but-non-colliding (deliberate); everything else clean.
VERIFY TOOLING (still valid): server on fresh port (confirm <title> is 真昼の月; never pkill -f "vite preview" — kills own shell). Driver scripts in ~/.cache/chrome-libs/driver/ — copy the puppeteer launch block (executablePath + env.LD_LIBRARY_PATH). Cinema pins + scrubbed dusk + rail-mode flips need SMALL REAL-SCROLL steps with waits; instant scrollTo/scrollIntoView does NOT advance ScrollTrigger or the active-section observer (this produced two false-alarm "findings" during review — always confirm with real scroll before treating a mid-pin/mid-scrub anomaly as a bug).

## DAY-3 PROGRESS (2026-07-07 ~08:40, Opus session) — historical, superseded by COMPLETE above
- Waves 1–5 COMMITTED (git a737f27) and CD-verified in-browser this session: S0 asymmetric organic wash (R1–R8 all landed), S1/S2 paper chapters w/ rotating-ink margin notes, S3 cinema (burn handoff, woven kanji, 覚醒 clip-path hero confirmed 醒-behind-figures, variant tablist, benched-single night rails), S4 sepia materials, S5 finale — dusk deepen + 6/6 moon assembly + obi-line completion + reader-card postcard (mailto ok). Deep-links land exact (#s4/#s5 top=0). No console errors across full journey. Mobile S0+S3 already good at 390px.
- Wave 6 (responsive/a11y/perf) IN FLIGHT — Sonnet agent launched ~08:40 (agentId a611def57ffd89370). Scope: rail→top-thread on mobile, per-section 390/768 fixes, keyboard/ARIA/contrast/alt/reduced-motion a11y sweep, <picture>+lazy+srcset perf. Then task #8 final CD review.
- Review tooling that WORKS this session: server on fresh port (4455/4477 etc — CONFIRM <title> is 真昼の月 first; never pkill -f "vite preview", it kills own shell). Driver scripts in ~/.cache/chrome-libs/driver/ (cd-review.mjs, cinema-dusk.mjs) — copy their puppeteer launch block (executablePath + env.LD_LIBRARY_PATH to downloaded libs). Cinema pins need small real-scroll steps, not instant jumps.

## NIGHT-2 PROGRESS (2026-07-07 ~04:20) — historical
- Wave 4 (S3 cinema) COMPLETE and CD-approved in-browser: 5 scenes, woven kanji, burn/un-burn, catalog chrome, keyboard variant switcher, night gaps w/ benched-single rails, 覚醒 hero clip-path FIXED (was silently broken: clipPath needs clip-rule not fill-rule) with hand-traced silhouette — verified 醒 passes behind figures.
- Wave 5 (S4 + S5 finale/postcard/moon payoff) BUILT and mostly verified (postcard + stamp + mailto %20-encoding pass; S4 warm paper + materials table pass). Three fixes IN FLIGHT with the Wave-5 agent: (1) BLOCKING deep-link drift — pins inflate doc after initial hash snap so /#s4 /#s5 land ~2 viewports early; re-snap after pin creation + each refresh; (2) rail S5 label 奥付 not "Finale"; (3) postcard microcopy ね! orphan wrap.
- Wave 6 (responsive/a11y/perf) NOT started, then final CD review. Bundle 58.17KB gz of 130KB.
- TOOLING UNLOCKED: headless Chromium now runs INSIDE WSL — binary at ~/.cache/ms-playwright/chromium_headless_shell-1228/chrome-headless-shell-linux64/chrome-headless-shell with LD_LIBRARY_PATH=~/.cache/chrome-libs/extracted/usr/lib/x86_64-linux-gnu (missing libs downloaded via apt-get download + dpkg -x, no root). puppeteer-core driver + working sweep scripts at ~/.cache/chrome-libs/driver/ (sweep.mjs, review2.mjs, review3.mjs). Full interaction testing (scroll, dwell, clicks, keyboard, seeded localStorage) — prefer this over Windows-Chrome one-shots, whose virtual-time mode starves Lenis and produces misleading blank shots. WSL still has no system JP fonts: tofu in shots = wrong (non-webfont) font stack bug, not an environment quirk.
- Moon payoff test recipe: seed localStorage mahiru.moons = ["s0"..."s5"] via evaluateOnNewDocument → 6/6 full-moon state.

## State (verified against filesystem 14:54, day 1)
- Assets DONE: 77-entry manifest, curated (chapters, S3 scenes s3-1…s3-5 + variants/editions, JP alts, 12 CD-authored margin notes, s2 interlude line in `tools/curation.json` _meta.copy-extensions). After any `npm run assets`, ALWAYS `node tools/curate.mjs`.
- Wave 2 (shell/tokens/motion infra/rail/moon/S0) DONE incl. an R1–R8 revision round (wash organicity, asymmetric S0, deep-link idempotence, mobile title scaling…) — revisions were applied by the 06:15 rescue session; NOT yet re-verified visually by the CD.
- Wave 3 (S1+S2 paper chapters) DONE per task list — plates.ts/plate.css exist, index.html 53KB with real content. NOT yet CD-reviewed.
- Wave 4 (S3 cinema) PARTIAL: src/scripts/cinema/{burn,switcher,scenes}.ts + cinema.css exist; the rescue session died mid-wave on a usage limit (09:58). Completeness unknown — AUDIT FIRST, then finish: burn/un-burn handoff, 5 pinned scenes w/ kanji depth, 覚醒 hero clip-path (醒 behind figures), catalog chrome §6.4, variant switcher §6.5, night gaps w/ vertical rails naming benched singles (list in curation _meta).
- Wave 5 (S4 roughs/materials + S5 finale/postcard/moon payoff) NOT started. Wave 6 (responsive/a11y/perf) NOT started. Then final CD review.
- Build passes: 57.20KB gz JS of 130KB budget. No git repo — working tree is the only state; consider `git init` + baseline commit first thing.
- Task list (harness): #5 in_progress = Wave 4. #6, #7, #8 pending.

## Immediate next steps for the resuming session
1. Serve on a FRESH port (4173 + 4174 belong to other projects/zombies; use e.g. 4321) and do the full CD screenshot sweep that was interrupted: S0 (verify R1–R8 landed: organic wash not rectangle, no stray blue, asymmetric lockup), S1 (real book-page wash, snow-umbrella centerpiece w/ silent UI accent, interactive archive board), S2 (season arc, margin notes dwell-revealing, interlude), S3 partial, mobile 390px, deep links.
2. Fix-or-dispatch anything failing; then launch Wave 4 completion agent (synchronous); review; Waves 5–6; final review.

## Verification recipe (no working browser inside WSL — libs missing, no sudo)
Windows Chrome one-shot:
`"/mnt/c/Program Files/Google/Chrome/Application/chrome.exe" --headless --disable-gpu --hide-scrollbars --window-size=1440,900 --virtual-time-budget=6000 --screenshot='C:\Temp\cc-shots\x.png' "http://localhost:PORT/"` then Read /mnt/c/Temp/cc-shots/x.png. `--force-prefers-reduced-motion` for reduced checks; 390,844 for mobile; URL fragments (#s1…#s5) to land on sections; tall --window-size (e.g. 1440,4000) samples long sections but inflates vh-units — judge composition, not spacing, in tall shots.
GOTCHAS: always confirm served `<title>` is 真昼の月 before judging screenshots (port 4173 serves an unrelated acid-pop "GYOKAI" site). Chrome may bind IPv6-only; prefer explicit fresh ports. Don't pkill vite in the same compound command that starts your own server.

## Ops
- Continuation scheduled: one-shot self-deregistering crontab at 2026-07-07 03:00 running `claude --continue` (resumes the latest ~/shiro conversation = the orchestration thread) with CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0. Date-guarded to 2026-07-07; skips if lock held. WSL must be awake at 01:00. Log: `.claude/logs/nightly-orchestrator.log`.
- Usage-limit history 2026-07-06: limits hit ~04:46 (reset 06:00), ~09:58 (reset 11:10). The 1 AM fresh window is the point of the night schedule.
